name: SUSFS Samsung 4.19 Stage 2

on:
  workflow_dispatch:
  push:
    branches:
      - ksun-3.3.0-ps5-susfs230
    paths:
      - '.github/workflows/susfs-stage2.yml'

jobs:
  stage2:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout exact kernel
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Verify baseline
        run: |
          set -euxo pipefail

          echo "HEAD:"
          git rev-parse HEAD

          echo "KernelSU:"
          git submodule status KernelSU-Next

          test "$(git -C KernelSU-Next rev-parse HEAD)" = \
            "3b18216f71df189ab3d1b1ce0bdb21be1268e771"

      - name: Fetch SUSFS 4.19 compatibility reference
        run: |
          set -euxo pipefail

          BASE="https://raw.githubusercontent.com/sch-lda/susfs4ksu-4.19-1.5.7/Branch_a/kernel_patches"

          curl -fL --retry 5 \
            "$BASE/50_add_susfs_in_kernel-4.19.patch" \
            -o /tmp/susfs419.patch

          curl -fL --retry 5 \
            "$BASE/fs/susfs.c" \
            -o /tmp/susfs.c

          curl -fL --retry 5 \
            "$BASE/fs/sus_su.c" \
            -o /tmp/sus_su.c

          curl -fL --retry 5 \
            "$BASE/include/linux/susfs.h" \
            -o /tmp/susfs.h

          curl -fL --retry 5 \
            "$BASE/include/linux/susfs_def.h" \
            -o /tmp/susfs_def.h

          curl -fL --retry 5 \
            "$BASE/include/linux/sus_su.h" \
            -o /tmp/sus_su.h

      - name: Recreate Stage 1 reference application
        run: |
          set -euxo pipefail

          set +e

          git apply \
            --reject \
            --whitespace=nowarn \
            /tmp/susfs419.patch

          RC=$?

          set -e

          echo "reference patch rc=$RC"

          cp /tmp/susfs.c fs/susfs.c
          cp /tmp/sus_su.c fs/sus_su.c

          cp /tmp/susfs.h include/linux/susfs.h
          cp /tmp/susfs_def.h include/linux/susfs_def.h
          cp /tmp/sus_su.h include/linux/sus_su.h

      - name: Recreate Samsung task_struct adaptation
        run: |
          set -euxo pipefail

          python3 - <<'PY'
          from pathlib import Path

          p = Path("include/linux/sched.h")
          s = p.read_text()

          if "u64 susfs_task_state;" not in s:
              marker = "randomized_struct_fields_end"

              idx = s.find(marker)

              if idx == -1:
                  raise SystemExit(
                      "randomized_struct_fields_end not found"
                  )

              insert = (
                  "#ifdef CONFIG_KSU_SUSFS\n"
                  "\tu64\t\t\t\tsusfs_task_state;\n"
                  "\tu64\t\t\t\tsusfs_last_fake_mnt_id;\n"
                  "#endif\n\n"
              )

              s = s[:idx] + insert + s[idx:]

              p.write_text(s)

          print("Samsung task_struct adaptation ready")
          PY

      - name: Recreate Samsung vfsmount adaptation
        run: |
          set -euxo pipefail

          python3 - <<'PY'
          from pathlib import Path

          p = Path("include/linux/mount.h")
          s = p.read_text()

          if "susfs_mnt_id_backup" not in s:
              marker = "\tint mnt_flags;\n"

              idx = s.find(marker)

              if idx == -1:
                  raise SystemExit(
                      "vfsmount mnt_flags marker not found"
                  )

              insert_at = idx + len(marker)

              insert = (
                  "#ifdef CONFIG_KSU_SUSFS\n"
                  "\tu64 susfs_mnt_id_backup;\n"
                  "#endif\n"
              )

              s = s[:insert_at] + insert + s[insert_at:]

              p.write_text(s)

          print("Samsung vfsmount adaptation ready")
          PY

      - name: Recreate include-only adaptations
        run: |
          set -euxo pipefail

          python3 - <<'PY'
          from pathlib import Path

          def add_after(path, anchor, block):
              p = Path(path)
              s = p.read_text()

              if "#include <linux/susfs_def.h>" in s:
                  return

              if anchor not in s:
                  raise SystemExit(
                      f"{path}: anchor missing: {anchor!r}"
                  )

              s = s.replace(
                  anchor,
                  anchor + "\n" + block,
                  1
              )

              p.write_text(s)

          add_after(
              "fs/dcache.c",
              "#include <linux/list_lru.h>",
              (
                  "#ifdef CONFIG_KSU_SUSFS_SUS_PATH\n"
                  "#include <linux/susfs_def.h>\n"
                  "#endif"
              )
          )

          add_after(
              "fs/namei.c",
              "#include <linux/build_bug.h>",
              (
                  "#if defined(CONFIG_KSU_SUSFS_SUS_PATH) || "
                  "defined(CONFIG_KSU_SUSFS_OPEN_REDIRECT)\n"
                  "#include <linux/susfs_def.h>\n"
                  "#endif"
              )
          )

          add_after(
              "fs/proc/task_mmu.c",
              "#include <linux/mm_inline.h>",
              (
                  "#ifdef CONFIG_KSU_SUSFS_SUS_KSTAT\n"
                  "#include <linux/susfs_def.h>\n"
                  "#endif"
              )
          )
          PY

      - name: Recreate Samsung namespace declarations
        run: |
          set -euxo pipefail

          python3 - <<'PY'
          from pathlib import Path

          p = Path("fs/namespace.c")
          s = p.read_text()

          if "#include <linux/susfs_def.h>" not in s:
              anchor = "#include <linux/sched/task.h>\n"

              if anchor not in s:
                  raise SystemExit(
                      "namespace include anchor missing"
                  )

              block = (
                  "#if defined(CONFIG_KSU_SUSFS_SUS_MOUNT) || "
                  "defined(CONFIG_KSU_SUSFS_TRY_UMOUNT)\n"
                  "#include <linux/susfs_def.h>\n"
                  "#endif\n"
              )

              s = s.replace(
                  anchor,
                  anchor + block,
                  1
              )

          if "static DEFINE_IDA(susfs_mnt_id_ida)" not in s:
              anchor = '#include "internal.h"\n'

              if anchor not in s:
                  raise SystemExit(
                      "namespace internal.h anchor missing"
                  )

              block = (
                  "\n"
                  "#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT\n"
                  "extern bool susfs_is_current_ksu_domain(void);\n"
                  "extern bool susfs_is_current_zygote_domain(void);\n"
                  "\n"
                  "static DEFINE_IDA(susfs_mnt_id_ida);\n"
                  "static DEFINE_IDA(susfs_mnt_group_ida);\n"
                  "\n"
                  "#define CL_COPY_MNT_NS BIT(25)\n"
                  "#endif\n"
                  "\n"
                  "#ifdef CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT\n"
                  "extern void susfs_auto_add_sus_ksu_default_mount(\n"
                  "    const char __user *to_pathname);\n"
                  "bool susfs_is_auto_add_sus_ksu_default_mount_enabled = true;\n"
                  "#endif\n"
                  "\n"
                  "#ifdef CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT\n"
                  "extern int susfs_auto_add_sus_bind_mount(\n"
                  "    const char *pathname,\n"
                  "    struct path *path_target);\n"
                  "bool susfs_is_auto_add_sus_bind_mount_enabled = true;\n"
                  "#endif\n"
                  "\n"
                  "#ifdef CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT\n"
                  "extern void susfs_auto_add_try_umount_for_bind_mount(\n"
                  "    struct path *path);\n"
                  "bool susfs_is_auto_add_try_umount_for_bind_mount_enabled = true;\n"
                  "#endif\n"
              )

              s = s.replace(
                  anchor,
                  anchor + block,
                  1
              )

          p.write_text(s)
          PY

      - name: Resolve Samsung namei rejects
        run: |
          set -euxo pipefail

          python3 - <<'PY'
          from pathlib import Path

          p = Path("fs/namei.c")
          s = p.read_text()

          if "#include <linux/susfs_def.h>" not in s:
              anchor = "#include <linux/build_bug.h>\n"

              if anchor not in s:
                  raise SystemExit(
                      "namei SUSFS include anchor missing"
                  )

              block = (
                  "#if defined(CONFIG_KSU_SUSFS_SUS_PATH) || "
                  "defined(CONFIG_KSU_SUSFS_OPEN_REDIRECT)\n"
                  "#include <linux/susfs_def.h>\n"
                  "#endif\n"
              )

              s = s.replace(
                  anchor,
                  anchor + block,
                  1
              )

          marker = (
              "static inline int may_create("
              "struct vfsmount *mnt, struct inode *dir, "
              "struct dentry *child)\n"
              "{\n"
          )

          if marker not in s:
              raise SystemExit(
                  "Samsung may_create(mnt, dir, child) anchor missing"
              )

          if "may_create_susfs_samsung419" not in s:
              replacement = (
                  marker
                  + "#ifdef CONFIG_KSU_SUSFS_SUS_PATH\n"
                  + "\t/* may_create_susfs_samsung419 */\n"
                  + "\tint susfs_error;\n"
                  + "#endif\n"
              )

              s = s.replace(
                  marker,
                  replacement,
                  1
              )

          audit_anchor = (
              "\taudit_inode_child("
              "dir, child, AUDIT_TYPE_CHILD_CREATE);\n"
          )

          if audit_anchor not in s:
              raise SystemExit(
                  "may_create audit anchor missing"
              )

          if "susfs_error = inode_permission2" not in s:
              block = (
                  "#ifdef CONFIG_KSU_SUSFS_SUS_PATH\n"
                  "\tif (child->d_inode &&\n"
                  "\t    unlikely(child->d_inode->i_state & "
                  "INODE_STATE_SUS_PATH) &&\n"
                  "\t    likely(current->susfs_task_state & "
                  "TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {\n"
                  "\t\tsusfs_error = inode_permission2("
                  "mnt, dir, MAY_WRITE | MAY_EXEC);\n"
                  "\t\tif (susfs_error)\n"
                  "\t\t\treturn susfs_error;\n"
                  "\n"
                  "\t\treturn -ENOENT;\n"
                  "\t}\n"
                  "#endif\n"
              )

              s = s.replace(
                  audit_anchor,
                  audit_anchor + block,
                  1
              )

          p.write_text(s)

          print("Resolved Samsung namei rejects")
          PY

      - name: Resolve Samsung inotify fdinfo reject
        run: |
          set -euxo pipefail

          python3 - <<'PY'
          from pathlib import Path

          p = Path("fs/notify/fdinfo.c")
          s = p.read_text()

          if "fdinfo_susfs_samsung419" in s:
              print("fdinfo SUSFS adaptation already present")
              raise SystemExit(0)

          # Stage 1 successfully applies the SUSFS callback plumbing:
          # under CONFIG_KSU_SUSFS_SUS_MOUNT the callback order is
          #   inotify_fdinfo(m, mark, file)
          # Only the Samsung-specific function-body hunk rejects.
          signature = (
              "static void inotify_fdinfo("
              "struct seq_file *m, struct fsnotify_mark *mark, "
              "struct file *file)"
          )

          start = s.find(signature)

          if start == -1:
              raise SystemExit(
                  "Expected Stage-1 inotify (m, mark, file) signature missing"
              )

          anchor = "\tif (inode) {\n"
          idx = s.find(anchor, start)

          if idx == -1:
              raise SystemExit(
                  "inotify inode block missing"
              )

          insert_at = idx + len(anchor)

          block = (
              "#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT\n"
              "\t\t/* fdinfo_susfs_samsung419 */\n"
              "\t\tif (likely(current->susfs_task_state & "
              "TASK_STRUCT_NON_ROOT_USER_APP_PROC) &&\n"
              "\t\t    unlikely(inode->i_state & "
              "INODE_STATE_SUS_KSTAT)) {\n"
              "\t\t\tstruct path susfs_path;\n"
              "\t\t\tchar *pathname;\n"
              "\t\t\tchar *dpath;\n"
              "\n"
              "\t\t\tpathname = kmalloc(PAGE_SIZE, GFP_KERNEL);\n"
              "\t\t\tif (!pathname)\n"
              "\t\t\t\tgoto susfs_fdinfo_fallback;\n"
              "\n"
              "\t\t\tdpath = d_path("
              "&file->f_path, pathname, PAGE_SIZE);\n"
              "\t\t\tif (IS_ERR(dpath)) {\n"
              "\t\t\t\tkfree(pathname);\n"
              "\t\t\t\tgoto susfs_fdinfo_fallback;\n"
              "\t\t\t}\n"
              "\n"
              "\t\t\tif (!kern_path(dpath, 0, &susfs_path)) {\n"
              "\t\t\t\tu32 susfs_mask = "
              "mark->mask & IN_ALL_EVENTS;\n"
              "\n"
              "\t\t\t\tseq_printf(m,\n"
              "\t\t\t\t\t\"inotify wd:%x ino:%lx "
              "sdev:%x mask:%x ignored_mask:%x \",\n"
              "\t\t\t\t\tinode_mark->wd,\n"
              "\t\t\t\t\tsusfs_path.dentry->d_inode->i_ino,\n"
              "\t\t\t\t\tsusfs_path.dentry->d_inode->"
              "i_sb->s_dev,\n"
              "\t\t\t\t\tsusfs_mask,\n"
              "\t\t\t\t\tmark->ignored_mask);\n"
              "\n"
              "\t\t\t\tshow_mark_fhandle(\n"
              "\t\t\t\t\tm,\n"
              "\t\t\t\t\tsusfs_path.dentry->d_inode);\n"
              "\t\t\t\tseq_putc(m, '\\n');\n"
              "\t\t\t\tpath_put(&susfs_path);\n"
              "\t\t\t\tkfree(pathname);\n"
              "\t\t\t\tiput(inode);\n"
              "\t\t\t\treturn;\n"
              "\t\t\t}\n"
              "\n"
              "\t\t\tkfree(pathname);\n"
              "\t\t}\n"
              "susfs_fdinfo_fallback:\n"
              "#endif\n"
          )

          s = s[:insert_at] + block + s[insert_at:]

          p.write_text(s)

          print("Resolved Samsung fdinfo reject")
          PY

      - name: Resolve Samsung overlayfs readdir reject
        run: |
          set -euxo pipefail

          python3 - <<'PY'
          from pathlib import Path

          p = Path("fs/overlayfs/readdir.c")
          s = p.read_text()

          if "ovl_susfs_samsung419" in s:
              print("overlayfs adaptation already present")
              raise SystemExit(0)

          function = "static int ovl_dir_open("

          start = s.find(function)

          if start == -1:
              raise SystemExit(
                  "ovl_dir_open not found"
              )

          anchor = (
              "\ttype = ovl_path_real("
              "file->f_path.dentry, &realpath);\n"
          )

          idx = s.find(anchor, start)

          if idx == -1:
              raise SystemExit(
                  "ovl_path_real anchor missing"
              )

          block = (
              "#ifdef CONFIG_KSU_SUSFS_SUS_OVERLAYFS\n"
              "\t/* ovl_susfs_samsung419 */\n"
              "\tovl_path_lowerdata("
              "file->f_path.dentry, &realpath);\n"
              "\tif (likely(realpath.mnt && realpath.dentry)) {\n"
              "\t\ttype = __OVL_PATH_UPPER;\n"
              "\t\tgoto susfs_ovl_open_real;\n"
              "\t}\n"
              "#endif\n"
              "\n"
          )

          s = s[:idx] + block + s[idx:]

          anchor2 = anchor

          idx2 = s.find(anchor2, idx + len(block))

          if idx2 == -1:
              raise SystemExit(
                  "second ovl path anchor missing"
              )

          insert_at = idx2 + len(anchor2)

          label = (
              "#ifdef CONFIG_KSU_SUSFS_SUS_OVERLAYFS\n"
              "susfs_ovl_open_real:\n"
              "#endif\n"
          )

          s = s[:insert_at] + label + s[insert_at:]

          p.write_text(s)

          print("Resolved Samsung overlayfs reject")
          PY

      - name: Resolve Samsung compat readdir reject
        run: |
          set -euxo pipefail

          python3 - <<'PY'
          from pathlib import Path

          p = Path("fs/readdir.c")
          s = p.read_text()

          if "compat_fillonedir_susfs_samsung419" in s:
              print("compat readdir adaptation already present")
              raise SystemExit(0)

          function = "static int compat_fillonedir("

          start = s.find(function)

          if start == -1:
              raise SystemExit(
                  "compat_fillonedir not found"
              )

          anchor = (
              "\tif (buf->result)\n"
              "\t\treturn -EINVAL;\n"
          )

          idx = s.find(anchor, start)

          if idx == -1:
              raise SystemExit(
                  "compat_fillonedir result anchor missing"
              )

          insert_at = idx + len(anchor)

          block = (
              "#ifdef CONFIG_KSU_SUSFS_SUS_PATH\n"
              "\t/* compat_fillonedir_susfs_samsung419 */\n"
              "\tif (likely(current->susfs_task_state & "
              "TASK_STRUCT_NON_ROOT_USER_APP_PROC) &&\n"
              "\t    susfs_sus_ino_for_filldir64(ino))\n"
              "\t\treturn 0;\n"
              "#endif\n"
          )

          s = s[:insert_at] + block + s[insert_at:]

          p.write_text(s)

          print("Resolved Samsung compat readdir reject")
          PY

      - name: Remove Stage 2 handled reject files
        run: |
          set -euxo pipefail

          rm -f \
            include/linux/sched.h.rej \
            include/linux/mount.h.rej \
            fs/dcache.c.rej \
            fs/proc/task_mmu.c.rej \
            fs/namei.c.rej \
            fs/notify/fdinfo.c.rej \
            fs/overlayfs/readdir.c.rej \
            fs/readdir.c.rej

          echo "=== REMAINING REJECT FILES ==="

          find . \
            -name '*.rej' \
            -print \
            | sort

      - name: Verify Stage 2 adaptations
        run: |
          set -euxo pipefail

          grep -n \
            "may_create_susfs_samsung419" \
            fs/namei.c

          grep -n \
            "fdinfo_susfs_samsung419" \
            fs/notify/fdinfo.c

          grep -n \
            "ovl_susfs_samsung419" \
            fs/overlayfs/readdir.c

          grep -n \
            "compat_fillonedir_susfs_samsung419" \
            fs/readdir.c

          grep -n \
            "susfs_task_state" \
            include/linux/sched.h

          grep -n \
            "susfs_mnt_id_backup" \
            include/linux/mount.h

          test -f fs/namespace.c.rej

          REJECT_COUNT="$(
            find . \
              -name '*.rej' \
              -type f \
              | wc -l
          )"

          echo "Remaining reject count: $REJECT_COUNT"

          test "$REJECT_COUNT" -eq 1

      - name: Generate Stage 2 patch
        run: |
          set -euxo pipefail

          git diff --binary \
            > /tmp/M127F-SUSFS-Stage2-Samsung419.patch

          git status --short \
            > /tmp/M127F-SUSFS-Stage2-status.txt

          find . \
            -name '*.rej' \
            -print \
            -exec cat {} \; \
            > /tmp/M127F-SUSFS-Stage2-remaining-rejects.txt

          git diff --stat \
            > /tmp/M127F-SUSFS-Stage2-diff-stat.txt

          echo "=== STAGE 2 DIFF STAT ==="
          cat /tmp/M127F-SUSFS-Stage2-diff-stat.txt

          echo
          echo "=== REMAINING REJECT ==="
          cat /tmp/M127F-SUSFS-Stage2-remaining-rejects.txt

      - name: Upload Stage 2 result
        uses: actions/upload-artifact@v4
        with:
          name: m127f-susfs-stage2-samsung419
          path: |
            /tmp/M127F-SUSFS-Stage2-Samsung419.patch
            /tmp/M127F-SUSFS-Stage2-status.txt
            /tmp/M127F-SUSFS-Stage2-remaining-rejects.txt
            /tmp/M127F-SUSFS-Stage2-diff-stat.txt
          if-no-files-found: error
          retention-days: 7
